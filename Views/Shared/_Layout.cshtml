@using manyasligida.Services
@inject CartService CartService
<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - Manyaslı Gıda</title>
    <meta name="description" content="Manyas'ın bereketli topraklarından sofralarınıza uzanan %100 doğal süt ürünleri. Katkısız, taze ve güvenilir üretimle sağlığınız için en iyisi.">
    <meta name="keywords" content="peynir, süt ürünleri, doğal, organik, manyas, gıda, taze">
    <link rel="icon" type="image/x-icon" href="~/favicon.ico" />

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">

    <!-- Font Awesome -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" rel="stylesheet">

    <!-- Google Fonts - Modern Typography -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&family=JetBrains+Mono:wght@400;500;600&display=swap" rel="stylesheet">

    <!-- Custom CSS -->
    <link href="~/css/site.css" rel="stylesheet" />
    <link href="~/css/style.css" rel="stylesheet" />

    <!-- AOS Animation -->
    <link href="https://unpkg.com/aos@2.3.1/dist/aos.css" rel="stylesheet">

    <!-- Owl Carousel -->
    <link rel="stylesheet" href="~/lib/owlcarousel/assets/owl.carousel.min.css">
    <link rel="stylesheet" href="~/lib/owlcarousel/assets/owl.theme.default.min.css">

    <style>
        :root {
            --primary-color: #e74c3c;
            --secondary-color: #f39c12;
            --accent-color: #f1c40f;
            --success-color: #27ae60;
            --info-color: #3498db;
            --warning-color: #f39c12;
            --danger-color: #e74c3c;
            --dark-color: #2c3e50;
            --light-color: #ecf0f1;
            --text-color: #2c3e50;
            --text-muted: #7f8c8d;
            --border-radius: 12px;
            --border-radius-lg: 20px;
            --shadow: 0 10px 30px rgba(0,0,0,0.1);
            --shadow-lg: 0 20px 60px rgba(0,0,0,0.15);
            --transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
            --gradient-primary: linear-gradient(135deg, #e74c3c 0%, #c0392b 100%);
            --gradient-secondary: linear-gradient(135deg, #f39c12 0%, #e67e22 100%);
            --shadow-primary: 0 8px 25px rgba(231, 76, 60, 0.3);
            --shadow-secondary: 0 8px 25px rgba(243, 156, 18, 0.3);
            --font-primary: 'Poppins', sans-serif;
            --text-main: #2c3e50;
            --text-muted: #7f8c8d;
            --bg-card: #ffffff;
            --bg-light: #f8f9fa;
            --transition-bounce: all 0.3s cubic-bezier(0.68, -0.55, 0.265, 1.55);
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Poppins', sans-serif;
            color: var(--text-color);
            line-height: 1.7;
            overflow-x: hidden;
        }

        /* Top Bar */
        .top-bar {
            background: var(--gradient-primary);
            color: white;
            padding: 8px 0;
            font-size: 0.85rem;
            font-weight: 500;
        }

            .top-bar a {
                color: rgba(255, 255, 255, 0.9);
                text-decoration: none;
                transition: var(--transition);
                padding: 4px 8px;
                border-radius: var(--border-radius);
            }

                .top-bar a:hover {
                    color: white;
                    background: rgba(255, 255, 255, 0.1);
                }

        /* Modern Enhancements */
        .glassmorphism {
            background: rgba(255, 255, 255, 0.25);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.18);
        }

        .modern-btn {
            position: relative;
            overflow: hidden;
            z-index: 1;
        }

            .modern-btn::before {
                content: '';
                position: absolute;
                top: 0;
                left: -100%;
                width: 100%;
                height: 100%;
                background: linear-gradient(90deg, transparent, rgba(255,255,255,0.2), transparent);
                transition: left 0.5s;
                z-index: -1;
            }

            .modern-btn:hover::before {
                left: 100%;
            }

        .floating-card {
            background: var(--bg-card);
            border-radius: var(--border-radius-lg);
            box-shadow: var(--shadow-lg);
            padding: 2rem;
            transition: var(--transition-bounce);
            border: 1px solid rgba(255,255,255,0.1);
        }

            .floating-card:hover {
                transform: translateY(-8px) scale(1.02);
                box-shadow: 0 25px 50px rgba(0,0,0,0.15);
            }

        /* Enhanced Buttons */
        .btn-modern {
            background: var(--gradient-primary);
            border: none;
            color: white;
            padding: 12px 30px;
            border-radius: 50px;
            font-weight: 600;
            position: relative;
            overflow: hidden;
            transition: var(--transition);
            text-transform: uppercase;
            letter-spacing: 0.5px;
            font-size: 0.9rem;
        }

            .btn-modern:hover {
                color: white;
                transform: translateY(-2px);
                box-shadow: var(--shadow-primary);
            }

            .btn-modern::after {
                content: '';
                position: absolute;
                top: 50%;
                left: 50%;
                width: 0;
                height: 0;
                border-radius: 50%;
                background: rgba(255,255,255,0.3);
                transform: translate(-50%, -50%);
                transition: width 0.6s, height 0.6s;
            }

            .btn-modern:active::after {
                width: 300px;
                height: 300px;
            }

        /* Header Styles */
        .header-fixed {
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            z-index: 1000;
            background: white;
            box-shadow: var(--shadow);
            height: 80px;
            transition: var(--transition);
        }

        /* Navbar Styles */
        .navbar {
            height: 100%;
            padding: 0;
            background: white;
        }

        .navbar-brand {
            display: flex;
            align-items: center;
            gap: 0.75rem;
            font-family: var(--font-primary);
            font-weight: 700;
            font-size: 1.5rem;
            color: var(--text-main);
            text-decoration: none;
            min-width: 200px;
        }

            .navbar-brand img {
                height: 45px;
                width: auto;
            }

            .navbar-brand span {
                color: var(--primary-color);
            }

        .navbar-nav {
            display: flex;
            align-items: center;
            gap: 0;
            margin: 0;
            padding: 0;
            list-style: none;
        }

        .nav-item {
            position: relative;
        }

        .nav-link {
            font-family: var(--font-primary);
            font-weight: 500;
            font-size: 0.95rem;
            color: var(--text-main);
            padding: 0.75rem 1.25rem;
            text-decoration: none;
            transition: var(--transition);
            white-space: nowrap;
            border-radius: var(--border-radius);
            margin: 0 0.25rem;
            display: block;
        }

            .nav-link:hover,
            .nav-link.active {
                color: var(--primary-color);
                background: rgba(231, 76, 60, 0.05);
            }

        /* Nav Right */
        .nav-right {
            display: flex;
            align-items: center;
            gap: 1rem;
            margin-left: 2rem;
        }

        .cart-link {
            position: relative;
            padding: 0.75rem;
            border-radius: var(--border-radius);
            transition: var(--transition);
        }

            .cart-link:hover {
                background: rgba(231, 76, 60, 0.05);
            }

        .cart-count {
            position: absolute;
            top: -5px;
            right: -5px;
            background: var(--primary-color);
            color: white;
            font-size: 0.7rem;
            font-weight: 600;
            padding: 2px 6px;
            border-radius: 10px;
            min-width: 18px;
            text-align: center;
        }

        /* User Menu */
        .user-menu {
            display: flex;
            align-items: center;
            gap: 15px;
        }

            .user-menu .btn {
                padding: 8px 16px;
                font-size: 0.9rem;
                border-radius: 25px;
            }

            .user-menu .dropdown-toggle::after {
                display: none;
            }

            .user-menu .dropdown-menu {
                border: none;
                box-shadow: var(--shadow-lg);
                border-radius: var(--border-radius);
                padding: 0.5rem 0;
            }

            .user-menu .dropdown-item {
                padding: 0.75rem 1.5rem;
                transition: var(--transition);
            }

                .user-menu .dropdown-item:hover {
                    background: rgba(231, 76, 60, 0.1);
                    color: var(--primary-color);
                }

        /* Mobile Menu */
        .navbar-toggler {
            display: none;
            padding: 0.5rem;
            border: none;
            background: none;
            color: var(--text-main);
            font-size: 1.2rem;
            cursor: pointer;
        }

            .navbar-toggler:focus {
                box-shadow: none;
                outline: none;
            }

        .navbar-collapse {
            display: flex;
            align-items: center;
            justify-content: space-between;
            flex-grow: 1;
        }

        @@media (max-width: 991px) {
            .navbar-toggler {
                display: block;
            }

            .navbar-collapse {
                position: absolute;
                top: 100%;
                left: 0;
                right: 0;
                background: white;
                box-shadow: var(--shadow-lg);
                border-radius: 0 0 var(--border-radius) var(--border-radius);
                padding: 1rem;
                display: none;
                flex-direction: column;
                align-items: stretch;
            }

                .navbar-collapse.show {
                    display: flex !important;
                }

            .navbar-nav {
                flex-direction: column;
                width: 100%;
                gap: 0.5rem;
                margin-bottom: 1rem;
            }

            .nav-link {
                padding: 0.75rem 1rem;
                width: 100%;
                display: block;
                text-align: center;
                margin: 0;
            }

            .nav-right {
                flex-direction: column;
                width: 100%;
                margin: 0;
                gap: 0.5rem;
            }

                .nav-right .nav-link {
                    width: 100%;
                    text-align: center;
                }

            .user-menu {
                flex-direction: column;
                width: 100%;
                gap: 0.5rem;
            }
        }

        /* Button Styles */
        .btn {
            border-radius: var(--border-radius-lg);
            font-family: var(--font-primary);
            font-weight: 600;
            padding: 14px 32px;
            transition: all var(--transition-bounce);
            border: none;
            text-decoration: none;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            gap: 12px;
            letter-spacing: -0.3px;
            position: relative;
            overflow: hidden;
            cursor: pointer;
            transform-origin: center;
            will-change: transform;
        }

            .btn i {
                font-size: 1.2em;
                transition: transform var(--transition);
            }

            .btn:hover i {
                transform: translateX(2px);
            }

            .btn::before {
                content: '';
                position: absolute;
                top: 0;
                left: 0;
                width: 100%;
                height: 100%;
                background: linear-gradient(135deg, rgba(255,255,255,0.2) 0%, rgba(255,255,255,0.05) 50%, rgba(255,255,255,0) 100% );
                transform: translateX(-100%) rotate(45deg);
                transition: transform 0.6s cubic-bezier(0.165, 0.84, 0.44, 1);
            }

            .btn:hover::before {
                transform: translateX(100%) rotate(45deg);
            }

        .btn-primary {
            background: var(--gradient-primary);
            color: white;
            box-shadow: var(--shadow-primary);
        }

            .btn-primary:hover {
                transform: translateY(-3px) scale(1.02);
                box-shadow: 0 12px 30px rgba(231, 76, 60, 0.35);
                color: white;
            }

            .btn-primary:active {
                transform: translateY(1px) scale(0.98);
            }

        .btn-secondary {
            background: var(--gradient-secondary);
            color: white;
            box-shadow: var(--shadow-secondary);
        }

            .btn-secondary:hover {
                transform: translateY(-3px) scale(1.02);
                box-shadow: 0 12px 30px rgba(243, 156, 18, 0.35);
                color: white;
            }

            .btn-secondary:active {
                transform: translateY(1px) scale(0.98);
            }

        .btn-outline-primary {
            border: 2px solid var(--primary-color);
            color: var(--primary-color);
            background: transparent;
            box-shadow: none;
            font-weight: 500;
        }

            .btn-outline-primary:hover {
                background: var(--gradient-primary);
                color: white;
                transform: translateY(-3px) scale(1.02);
                box-shadow: var(--shadow-primary);
                border-color: transparent;
            }

            .btn-outline-primary:active {
                transform: translateY(1px) scale(0.98);
            }

        .btn-outline-light {
            border: 2px solid rgba(255, 255, 255, 0.8);
            color: white;
            background: transparent;
        }

            .btn-outline-light:hover {
                background: white;
                color: var(--primary-color);
                transform: translateY(-3px) scale(1.02);
                box-shadow: 0 12px 30px rgba(255, 255, 255, 0.3);
            }

        /* Card Styles */
        .card {
            border: none;
            border-radius: var(--border-radius-lg);
            box-shadow: var(--shadow);
            transition: var(--transition);
            overflow: hidden;
            background: var(--bg-card);
            border: 1px solid rgba(0,0,0,0.05);
        }

            .card:hover {
                transform: translateY(-5px) scale(1.01);
                box-shadow: var(--shadow-lg);
                border-color: rgba(0,0,0,0.08);
            }

        .card-img-top {
            height: 240px;
            object-fit: cover;
            transition: var(--transition);
        }

        .card:hover .card-img-top {
            transform: scale(1.08);
        }

        .card-body {
            padding: 1.8rem;
        }

        .card-title {
            font-family: var(--font-primary);
            font-weight: 700;
            font-size: 1.25rem;
            margin-bottom: 1rem;
            color: var(--text-main);
            letter-spacing: -0.3px;
        }

        .card-text {
            color: var(--text-muted);
            line-height: 1.7;
            margin-bottom: 1.5rem;
        }

        /* Section Styles */
        .section-padding {
            padding: 100px 0;
            position: relative;
            overflow: hidden;
        }

        .section-title {
            text-align: center;
            margin-bottom: 70px;
            position: relative;
        }

            .section-title::before {
                content: '';
                position: absolute;
                bottom: -15px;
                left: 50%;
                transform: translateX(-50%);
                width: 80px;
                height: 4px;
                background: var(--gradient-primary);
                border-radius: 2px;
            }

            .section-title h2 {
                font-family: var(--font-primary);
                font-size: 2.75rem;
                font-weight: 800;
                color: var(--text-main);
                margin-bottom: 20px;
                letter-spacing: -1px;
                line-height: 1.2;
            }

            .section-title p {
                font-size: 1.15rem;
                color: var(--text-muted);
                max-width: 700px;
                margin: 0 auto;
                line-height: 1.8;
            }

        /* Hero Section */
        .hero-section {
            min-height: 100vh;
            display: flex;
            align-items: center;
            position: relative;
            overflow: hidden;
        }

        .video-background {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: 1;
        }

        .hero-video {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }

        .video-overlay {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(135deg, rgba(44, 62, 80, 0.8) 0%, rgba(231, 76, 60, 0.6) 100%);
        }

        .z-index-2 {
            position: relative;
            z-index: 2;
        }

        /* Floating Elements */
        .floating-elements {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: 1;
            pointer-events: none;
        }

        .floating-element {
            position: absolute;
            width: 60px;
            height: 60px;
            background: rgba(255, 255, 255, 0.1);
            border-radius: 50%;
            animation: float 6s ease-in-out infinite;
        }

        @@keyframes float {
            0%, 100% {
                transform: translateY(0px) rotate(0deg);
            }

            50% {
                transform: translateY(-20px) rotate(180deg);
            }
        }

        /* Scroll Indicator */
        .scroll-indicator {
            position: absolute;
            bottom: 30px;
            left: 50%;
            transform: translateX(-50%);
            z-index: 2;
        }

        .scroll-arrow {
            width: 30px;
            height: 30px;
            border: 2px solid white;
            border-top: none;
            border-left: none;
            transform: rotate(45deg);
            animation: scroll 2s infinite;
        }

        @@keyframes scroll {
            0% {
                opacity: 1;
                transform: rotate(45deg) translateY(0);
            }

            50% {
                opacity: 0.5;
                transform: rotate(45deg) translateY(10px);
            }

            100% {
                opacity: 1;
                transform: rotate(45deg) translateY(0);
            }
        }

        /* Cart Badge */
        .cart-badge {
            position: relative;
        }

        .cart-count {
            position: absolute;
            top: -8px;
            right: -8px;
            background: var(--danger-color);
            color: white;
            border-radius: 50%;
            width: 20px;
            height: 20px;
            font-size: 12px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: 600;
        }

        /* Toast Notifications */
        .toast-container {
            position: fixed;
            top: 20px;
            right: 20px;
            z-index: 9999;
        }

        .toast {
            background: white;
            border-radius: var(--border-radius);
            box-shadow: var(--shadow-lg);
            border: none;
            margin-bottom: 10px;
        }

        /* Footer */
        .footer {
            background: linear-gradient(135deg, var(--dark-color) 0%, #34495e 100%);
            color: white;
            padding: 80px 0 30px;
            position: relative;
            overflow: hidden;
        }

            .footer::before {
                content: '';
                position: absolute;
                top: 0;
                left: 0;
                width: 100%;
                height: 100%;
                background: url("data:image/svg+xml,%3Csvg width='60' height='60' viewBox='0 0 60 60' xmlns='http://www.w3.org/2000/svg'%3E%3Cg fill='none' fill-rule='evenodd'%3E%3Cg fill='%23ffffff' fill-opacity='0.05'%3E%3Cpath d='M36 34v-4h-2v4h-4v2h4v4h2v-4h4v-2h-4zm0-30V0h-2v4h-4v2h4v4h2V6h4V4h-4zM6 34v-4H4v4H0v2h4v4h2v-4h4v-2H6zM6 4V0H4v4H0v2h4v4h2V6h4V4H6z'/%3E%3C/g%3E%3C/g%3E%3C/svg%3E");
                opacity: 0.1;
            }

            .footer h5 {
                color: var(--accent-color);
                font-family: var(--font-primary);
                font-weight: 700;
                margin-bottom: 25px;
                font-size: 1.25rem;
                letter-spacing: -0.3px;
                position: relative;
                display: inline-block;
            }

                .footer h5::after {
                    content: '';
                    position: absolute;
                    bottom: -8px;
                    left: 0;
                    width: 40px;
                    height: 3px;
                    background: var(--gradient-primary);
                    border-radius: 2px;
                }

            .footer a {
                color: rgba(255, 255, 255, 0.7);
                text-decoration: none;
                transition: var(--transition);
                display: inline-flex;
                align-items: center;
                gap: 8px;
                padding: 6px 0;
            }

                .footer a:hover {
                    color: var(--accent-color);
                    transform: translateX(5px);
                }

        .footer-bottom {
            border-top: 1px solid rgba(255, 255, 255, 0.1);
            padding-top: 25px;
            margin-top: 50px;
            text-align: center;
            position: relative;
        }

        .footer .social-links a {
            width: 38px;
            height: 38px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            border-radius: 50%;
            background: rgba(255, 255, 255, 0.1);
            color: white;
            font-size: 1.1rem;
            transition: var(--transition);
            margin-right: 10px;
        }

            .footer .social-links a:hover {
                background: var(--gradient-primary);
                transform: translateY(-3px);
                color: white;
            }

        /* Mobile-First Responsive Design */
        @@media (max-width: 992px) {
            .floating-card {
                padding: 1.5rem;
                margin-bottom: 1rem;
            }

            .btn-modern {
                padding: 10px 20px;
                font-size: 0.85rem;
            }
        }

        @@media (max-width: 768px) {
            .navbar-nav .nav-link {
                margin: 5px 0;
                text-align: center;
            }

            .section-title h2 {
                font-size: 2rem;
            }

            .hero-section {
                min-height: 80vh;
            }

            .user-menu {
                flex-direction: column;
                gap: 10px;
                margin-top: 15px;
            }

            .header-fixed {
                height: 70px;
            }

            .navbar-brand {
                font-size: 1.2rem;
            }

                .navbar-brand img {
                    height: 35px;
                }
        }

        /* Loading Animation */
        .loading {
            display: inline-block;
            width: 20px;
            height: 20px;
            border: 3px solid rgba(255,255,255,.3);
            border-radius: 50%;
            border-top-color: #fff;
            animation: spin 1s ease-in-out infinite;
        }

        @@keyframes spin {
            to {
                transform: rotate(360deg);
            }
        }
    </style>
</head>
<body>
    <!-- Toast Container -->
    <div class="toast-container"></div>

    <!-- Top Bar -->
    <div class="top-bar">
        <div class="container">
            <div class="row align-items-center">
                <div class="col-md-6">
                    <div class="d-flex align-items-center gap-3">
                        <span><i class="fas fa-phone me-1"></i>+90 266 123 45 67</span>
                        <span><i class="fas fa-envelope me-1"></i>info@manyasligida.com</span>
                    </div>
                </div>
                <div class="col-md-6 text-end">
                    <div class="d-flex align-items-center justify-content-end gap-3">
                        <a href="#"><i class="fab fa-facebook-f"></i></a>
                        <a href="#"><i class="fab fa-instagram"></i></a>
                        <a href="#"><i class="fab fa-twitter"></i></a>
                        <a href="#"><i class="fab fa-youtube"></i></a>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Navigation -->
    <header class="header-fixed">
        <nav class="navbar navbar-expand-lg">
            <div class="container">
                <a class="navbar-brand" href="@Url.Action("Index", "Home")">
                    <img src="~/img/manyasli-gida.png" alt="Manyaslı Gıda" />

                    <span>Manyaslı Gıda</span>
                </a>

                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                    <i class="fas fa-bars"></i>
                </button>

                <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar-nav mx-auto">
                        <li class="nav-item">
                            <a class="nav-link @(ViewContext.RouteData.Values["Controller"]?.ToString() == "Home" && ViewContext.RouteData.Values["Action"]?.ToString() == "Index" ? "active" : "")" href="@Url.Action("Index", "Home")">
                                <i class="fas fa-home me-1"></i>Ana Sayfa
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link @(ViewContext.RouteData.Values["Controller"]?.ToString() == "Products" ? "active" : "")" href="@Url.Action("Index", "Products")">
                                <i class="fas fa-box me-1"></i>Ürünler
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link @(ViewContext.RouteData.Values["Controller"]?.ToString() == "About" ? "active" : "")" href="@Url.Action("Index", "About")">
                                <i class="fas fa-info-circle me-1"></i>Hakkımızda
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link @(ViewContext.RouteData.Values["Controller"]?.ToString() == "Blog" ? "active" : "")" href="@Url.Action("Index", "Blog")">
                                <i class="fas fa-blog me-1"></i>Blog
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link @(ViewContext.RouteData.Values["Controller"]?.ToString() == "FAQ" ? "active" : "")" href="@Url.Action("Index", "FAQ")">
                                <i class="fas fa-question-circle me-1"></i>SSS
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link @(ViewContext.RouteData.Values["Controller"]?.ToString() == "Contact" ? "active" : "")" href="@Url.Action("Index", "Contact")">
                                <i class="fas fa-envelope me-1"></i>İletişim
                            </a>
                        </li>
                    </ul>

                    <div class="nav-right">
                        @if (Context.Session.GetString("UserId") != null)
                        {
                            <div class="dropdown">
                                <button class="btn btn-link nav-link dropdown-toggle" type="button" data-bs-toggle="dropdown">
                                    <i class="fas fa-user"></i>
                                    <span>@Context.Session.GetString("UserName")</span>
                                </button>
                                <ul class="dropdown-menu dropdown-menu-end">
                                    <li><a class="dropdown-item" href="@Url.Action("Profile", "Account")">Profilim</a></li>
                                    <li><a class="dropdown-item" href="@Url.Action("Index", "Order")">Siparişlerim</a></li>
                                    <li><hr class="dropdown-divider"></li>
                                    <li><a class="dropdown-item" href="@Url.Action("Logout", "Account")">Çıkış Yap</a></li>
                                </ul>
                            </div>
                        }
                        else
                        {
                            <a href="@Url.Action("Login", "Account")" class="nav-link">Giriş Yap</a>
                            <a href="@Url.Action("Register", "Account")" class="nav-link">Üye Ol</a>
                        }

                        <a class="nav-link cart-link" href="@Url.Action("Index", "Cart")">
                            <i class="fas fa-shopping-cart"></i>
                            <span class="cart-count">@CartService.GetCartItemCount()</span>
                        </a>
                    </div>
                </div>
            </div>
        </nav>
    </header>

    <!-- Main Content -->
    <main role="main" style="margin-top: 80px;">
        @RenderBody()
    </main>

    <!-- Footer -->
    <footer class="footer">
        <div class="container">
            <div class="row">
                <div class="col-lg-4 col-md-6 mb-4">
                    <h5><i class="fas fa-store me-2"></i>Manyaslı Gıda</h5>
                    <p class="mb-3">Manyas'ın bereketli topraklarından sofralarınıza uzanan %100 doğal süt ürünleri. Katkısız, taze ve güvenilir üretimle sağlığınız için en iyisi.</p>
                    <div class="social-links">
                        @{
                            var socialSettings = ViewBag.SiteSettings as dynamic ?? new { FacebookUrl = "#", InstagramUrl = "#", TwitterUrl = "#", YoutubeUrl = "#" };
                        }
                        <a href="@socialSettings.FacebookUrl" class="me-3"><i class="fab fa-facebook-f"></i></a>
                        <a href="@socialSettings.InstagramUrl" class="me-3"><i class="fab fa-instagram"></i></a>
                        <a href="@socialSettings.TwitterUrl" class="me-3"><i class="fab fa-twitter"></i></a>
                        <a href="@socialSettings.YoutubeUrl" class="me-3"><i class="fab fa-youtube"></i></a>
                    </div>
                </div>

                <div class="col-lg-2 col-md-6 mb-4">
                    <h5>Ürün Kategorileri</h5>
                    <ul class="list-unstyled">
                        @{
                            var footerCategories = ViewBag.Categories as List<manyasligida.Models.Category> ?? new List<manyasligida.Models.Category>();
                        }
                        @foreach (var category in footerCategories.Take(3))
                        {
                            <li><a href="@Url.Action("Category", "Products", new { id = category.Id })">@category.Name</a></li>
                        }
                        <li><a href="@Url.Action("Index", "Products")">Tüm Ürünler</a></li>
                    </ul>
                </div>

                <div class="col-lg-3 col-md-6 mb-4">
                    <h5>Hızlı Linkler</h5>
                    <ul class="list-unstyled">
                        <li><a href="@Url.Action("Index", "About")">Hakkımızda</a></li>
                        <li><a href="@Url.Action("Index", "Blog")">Blog</a></li>
                        <li><a href="@Url.Action("Index", "FAQ")">Sıkça Sorulan Sorular</a></li>
                    </ul>
                </div>

                <div class="col-lg-3 col-md-6 mb-4">
                    <h5>İletişim Bilgileri</h5>
                    <ul class="list-unstyled">
                        @{
                            var siteSettings = ViewBag.SiteSettings as dynamic ?? new { Address = "Manyas, Balıkesir", Phone = "+90 266 123 45 67", Email = "info@manyasligida.com", WorkingHours = "Pzt-Cmt: 08:00-18:00" };
                        }
                        <li><i class="fas fa-map-marker-alt me-2"></i>@siteSettings.Address</li>
                        <li><i class="fas fa-phone me-2"></i>@siteSettings.Phone</li>
                        <li><i class="fas fa-envelope me-2"></i>@siteSettings.Email</li>
                        <li><i class="fas fa-clock me-2"></i>@siteSettings.WorkingHours</li>
                    </ul>
                </div>
            </div>

            <div class="footer-bottom">
                <div class="row align-items-center">
                    <div class="col-md-6">
                        <p class="mb-0">&copy; 2024 Manyaslı Gıda. Tüm hakları saklıdır.</p>
                    </div>
                    <div class="col-md-6 text-md-end">
                        <a href="@Url.Action("Index", "KVKK")" class="me-3">KVKK</a>
                        <a href="@Url.Action("Gizlilik", "KVKK")" class="me-3">Gizlilik Politikası</a>
                        <a href="@Url.Action("Index", "FAQ")">SSS</a>
                    </div>
                </div>
            </div>
        </div>
    </footer>

    <!-- Scripts -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://code.jquery.com/jquery-3.7.1.min.js"></script>
    <script src="https://unpkg.com/aos@2.3.1/dist/aos.js"></script>
    <script src="~/lib/owlcarousel/owl.carousel.min.js"></script>
    <script src="~/js/site.js"></script>

    <script>
        // Initialize AOS
        AOS.init({
            duration: 1000,
            once: true,
            offset: 100
        });

        // Navbar scroll effect
        $(window).scroll(function() {
            if ($(window).scrollTop() > 50) {
                $('.header-fixed').addClass('scrolled');
            } else {
                $('.header-fixed').removeClass('scrolled');
            }
        });

        // Toast notification function
        function showToast(message, type = 'info') {
            const toast = $(`
                <div class="toast" role="alert">
                    <div class="toast-header">
                        <i class="fas fa-${type === 'success' ? 'check-circle text-success' : type === 'error' ? 'exclamation-circle text-danger' : 'info-circle text-info'} me-2"></i>
                        <strong class="me-auto">Bildirim</strong>
                        <button type="button" class="btn-close" data-bs-dismiss="toast"></button>
                    </div>
                    <div class="toast-body">
                        ${message}
                    </div>
                </div>
            `);

            $('.toast-container').append(toast);
            const bsToast = new bootstrap.Toast(toast[0]);
            bsToast.show();

            toast.on('hidden.bs.toast', function() {
                $(this).remove();
            });
        }

        // Cart count update function
        function updateCartCount() {
            $.get('/Cart/GetCartCount')
                .done(function(response) {
                    $('.cart-count').text(response.cartItemCount);
                });
        }

        // Add to cart function
        function addToCart(productId, quantity = 1) {
                            const btn = $('[data-product-id="' + productId + '"]');
            const originalText = btn.html();

            btn.prop('disabled', true).html('<span class="loading"></span>');

            $.post('/Cart/AddToCart', { productId: productId, quantity: quantity })
                .done(function(response) {
                    if (response.success) {
                        updateCartCount();
                        showToast(response.message, 'success');
                    } else {
                        showToast(response.message, 'error');
                    }
                })
                .fail(function() {
                    showToast('Bir hata oluştu', 'error');
                })
                .always(function() {
                    btn.prop('disabled', false).html(originalText);
                });
        }

        // Mobile menu functionality
        $(document).ready(function() {
            // Mobile menu toggle
            $('.navbar-toggler').on('click', function() {
                $('.navbar-collapse').toggleClass('show');
            });

            // Close mobile menu when clicking on a link
            $('.navbar-nav .nav-link').on('click', function() {
                $('.navbar-collapse').removeClass('show');
            });

            // Close mobile menu when clicking outside
            $(document).on('click', function(e) {
                if (!$(e.target).closest('.navbar').length) {
                    $('.navbar-collapse').removeClass('show');
                }
            });
        });

        // Show TempData messages
        @if (TempData["Success"] != null)
        {
                <text>showToast('@Html.Raw(TempData["Success"])', 'success');</text>
        }
        @if (TempData["Error"] != null)
        {
                <text>showToast('@Html.Raw(TempData["Error"])', 'error');</text>
        }
        @if (TempData["Info"] != null)
        {
                <text>showToast('@Html.Raw(TempData["Info"])', 'info');</text>
        }
    </script>

    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>
